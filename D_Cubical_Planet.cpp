#include<bits/stdc++.h>
using namespace std;
using ll = long long;
#define opt() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define yes cout<<"YES"<<endl;
#define no cout<<"NO"<<endl;

void solve ()
{
   int a,b,c,x,y,z;

   cin>>a>>b>>c>>x>>y>>z;

   (a != x && b != y && c != z) ? cout<<"NO"<<endl : cout<<"YES"<<endl ;
}

int main ()
{
    opt();
    
        solve();
    
}